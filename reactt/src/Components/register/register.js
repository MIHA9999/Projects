import React from "react";

class Register extends React.Component {
    
    render() {
        return (
            <button onClick={this.props.caller}>TRANSFER</button>
        );
    }
}
export default Register;